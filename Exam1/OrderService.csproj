<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework> <!-- You can change this to a later version like net7.0 if needed -->
    <PublishSingleFile>true</PublishSingleFile> <!-- Ensure the output is a single file -->
    <SelfContained>false</SelfContained> <!-- Optional: Set to true if you want a self-contained app (no .NET runtime required) -->
  </PropertyGroup>

</Project>
